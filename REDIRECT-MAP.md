# Redirect map — old URLs → new URLs

When the site goes live (or on any host that supports redirects), 301 each old
path to its new home. One line per move: `old → new`.

## 2026-07 structural pass — single-route cities merged (hub + route → one page)
/utrecht/city-centre/ → /utrecht/
/amsterdam/ndsm-noord/ → /amsterdam/
/groningen/gin-walk/ → /groningen/
